/**
 *
 * @type {{serviceCall: set of objects that we might get back from back end serviceCall}}
 * @usage var appConfig = require('application-configuration')();
 *      var serviceCallResponse = appConfig.mockData.get('/serviceCall');
 *
 * Note: This file can be edited directly from gitlab by BA Team

 */
module.exports = {
    serviceCall: {
        servicesResponseAttribute1: "value1",
        servicesResponseAttribute2: "value2"
    }
}
